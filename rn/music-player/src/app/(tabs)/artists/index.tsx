import { defaultStyles } from '@/styles'
import { View, Text } from 'react-native'

const ArtlistsScreen = () => {
  return (
    <View style={defaultStyles.container}>
      <Text style={defaultStyles.text}>Artlists Screen</Text>
    </View>
  )
}

export default ArtlistsScreen
